scipy  # required for spline interpolation

torchmetrics ~= 0.11.0
open3d ~= 0.17.0
dgl == 1.1.2  # deep graph learning lib required by pc-skeletor 0.0.6 that depends on pytorch > 2.0
pc-skeletor == 0.0.6  # API changes with version 1.0.0

hydra-core ~= 1.3.0
wandb
