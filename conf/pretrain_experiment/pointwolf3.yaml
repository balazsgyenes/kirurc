# @package _global_
# w/o VariableJitter, w/ PointWOLF

defaults:
  - override /dataset/augmentation@synthetic_dataset.augmentation: synthetic_pointwolf

synthetic_dataset:
  augmentation:
    vary_jitter: False

wandb:
  name: w/o VariableJitter, w/ PointWOLF
